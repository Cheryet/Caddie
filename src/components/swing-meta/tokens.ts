/**
 * tokens — internal chrome tokens shared by swing-meta controls
 * These are the translucent surface colors that overlay the camera
 * preview. They render correctly on any dark surface, so the same
 * controls work in CameraScreen (over a live preview) and in the
 * ImportConfirmSheet (over `bg.elevated`).
 */

export const CHROME_BG = 'rgba(12,12,12,0.6)';
export const CHROME_BORDER = 'rgba(255,255,255,0.1)';
export const PILL_INACTIVE_LABEL = 'rgba(240,237,232,0.7)';
