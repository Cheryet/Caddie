/**
 * components/swing-meta — Barrel export
 * Cross-feature controls for collecting swing metadata (camera angle,
 * swing hand, club). Used by both CameraScreen and ImportConfirmSheet.
 *
 *   import { AngleSegmented, HandSegmented, ClubChips } from '@/components/swing-meta';
 */

export { AngleSegmented } from './AngleSegmented';
export { HandSegmented } from './HandSegmented';
export { ClubChips } from './ClubChips';
